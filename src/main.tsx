import React from 'react'
import ReactDOM from 'react-dom/client'
import CoworkerROICalculator from './coworker-roi-calculator.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CoworkerROICalculator />
  </React.StrictMode>,
)
